{
  "title": "Config File Schema",
  "description": "Schema for config file read by the backend",
  "type": "object",
  "properties": {
    "user": {
      "type": "object",
      "properties": {
        "name": { "type": "string" }
      },
      "additionalProperties": false
    },
    "settings": {
      "type": "object",
      "properties": {
        "databaseUri": { "type": "string" },
        "language": { "$ref": "#/$defs/LanguageData" },
        "logLevel": { "$ref": "#/$defs/LogLevel" },
        "theme": { "$ref": "#/$defs/SupportedThemes" },
        "todayAutoscrollLeftOffset": { "type": "number" },
        "yearChangeScrollBegin": { "type": "boolean" },
        "yearToShow": {
          "type": "number",
          "minimum": 1900
        }
      },
      "required": [
        "language",
        "logLevel",
        "theme",
        "todayAutoscrollLeftOffset",
        "yearChangeScrollBegin"
      ],
      "additionalProperties": false
    }
  },
  "required": ["settings"],
  "additionalProperties": false,
  "$defs": {
    "LogLevel": {
      "type": "string",
      "enum": ["TRACE", "DEBUG", "INFO", "WARNING", "ERROR"]
    },
    "SupportedLanguages": {
      "type": "string",
      "enum": ["de-DE", "en-US"]
    },
    "SupportedLanguagesShort": {
      "type": "string",
      "enum": ["deDE", "enUS"]
    },
    "SupportedThemes": {
      "type": "string",
      "enum": ["dark", "light"]
    },
    "LanguageData": {
      "type": "object",
      "properties": {
        "dateMask": { "type": "string" },
        "importName": { "$ref": "#/$defs/SupportedLanguagesShort" },
        "locale": { "$ref": "#/$defs/SupportedLanguages" },
        "name": { "type": "string" }
      },
      "required": ["dateMask", "importName", "locale", "name"],
      "additionalProperties": false
    }
  }
}
